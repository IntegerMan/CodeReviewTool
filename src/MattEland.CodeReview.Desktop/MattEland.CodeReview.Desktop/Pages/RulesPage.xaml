<Page x:Class="MattEland.CodeReview.Desktop.Pages.RulesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MattEland.CodeReview.Desktop.Pages"
      xmlns:vm="using:MattEland.CodeReview.Desktop.ViewModels"
      xmlns:controls="using:MattEland.CodeReview.Desktop.Controls"
      xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="4">
                <TextBlock Text="Analysis Rules"
                           Style="{ThemeResource TitleLargeTextBlockStyle}"/>
                <TextBlock Style="{ThemeResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                    <Run Text="Showing"/>
                    <Run Text="{x:Bind ViewModel.FilteredRuleCount, Mode=OneWay}" FontWeight="SemiBold"/>
                    <Run Text="of"/>
                    <Run Text="{x:Bind ViewModel.TotalRuleCount, Mode=OneWay}" FontWeight="SemiBold"/>
                    <Run Text="rules ("/>
                    <Run Text="{x:Bind ViewModel.EnabledRuleCount, Mode=OneWay}"/>
                    <Run Text="enabled)"/>
                </TextBlock>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Refresh Rules"
                    Command="{x:Bind ViewModel.LoadRulesCommand}"
                    VerticalAlignment="Center">
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F5"/>
                </Button.KeyboardAccelerators>
            </Button>
        </Grid>

        <!-- Filters -->
        <Border Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Language Filter -->
                <StackPanel Grid.Column="0" Spacing="4">
                    <TextBlock Text="Language"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <ComboBox ItemsSource="{x:Bind ViewModel.Languages, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.SelectedLanguage, Mode=TwoWay}"
                              MinWidth="120"/>
                </StackPanel>

                <!-- Category Filter -->
                <StackPanel Grid.Column="1" Spacing="4">
                    <TextBlock Text="Category"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <ComboBox ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.SelectedCategory, Mode=TwoWay}"
                              MinWidth="140"/>
                </StackPanel>

                <!-- Search Box -->
                <StackPanel Grid.Column="2" Spacing="4">
                    <TextBlock Text="Search"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <AutoSuggestBox QueryIcon="Find"
                                    PlaceholderText="Search rules..."
                                    Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- Clear Filters -->
                <Button Grid.Column="3"
                        Content="Clear Filters"
                        Command="{x:Bind ViewModel.ClearFiltersCommand}"
                        VerticalAlignment="Bottom"/>
            </Grid>
        </Border>

        <!-- Rules List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <muxc:ItemsRepeater ItemsSource="{x:Bind ViewModel.Rules, Mode=OneWay}">
                <muxc:ItemsRepeater.Layout>
                    <muxc:StackLayout Spacing="8"/>
                </muxc:ItemsRepeater.Layout>
                <muxc:ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="vm:RuleViewModel">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16">
                            <Grid ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Severity Indicator -->
                                <Border Grid.Column="0"
                                        Width="4"
                                        CornerRadius="2"
                                        VerticalAlignment="Stretch">
                                    <Border.Background>
                                        <SolidColorBrush Color="{x:Bind local:RulesPage.GetSeverityColor(DefaultSeverity)}"/>
                                    </Border.Background>
                                </Border>

                                <!-- Rule Info -->
                                <StackPanel Grid.Column="1" Spacing="6">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind Name}"
                                                   Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                        
                                        <!-- Language/Category Tags -->
                                        <Border Background="{ThemeResource SystemAccentColorLight2}"
                                                CornerRadius="4"
                                                Padding="6,2">
                                            <TextBlock Text="{x:Bind Language}"
                                                       Style="{ThemeResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource SystemAccentColorDark1}"/>
                                        </Border>
                                        <Border Background="{ThemeResource ControlFillColorSecondaryBrush}"
                                                CornerRadius="4"
                                                Padding="6,2">
                                            <TextBlock Text="{x:Bind Category}"
                                                       Style="{ThemeResource CaptionTextBlockStyle}"/>
                                        </Border>
                                        
                                        <!-- Embedded indicator -->
                                        <FontIcon Glyph="&#xE8F1;" 
                                                  FontSize="12"
                                                  ToolTipService.ToolTip="Built-in rule"
                                                  Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                  Visibility="{x:Bind local:RulesPage.ToVisibility(IsEmbedded)}"/>
                                    </StackPanel>

                                    <TextBlock Text="{x:Bind Description}"
                                               Style="{ThemeResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextWrapping="WrapWholeWords"
                                               MaxLines="2"/>

                                    <!-- Tags -->
                                    <muxc:ItemsRepeater ItemsSource="{x:Bind Tags}" Visibility="{x:Bind local:RulesPage.VisibleIfAny(Tags)}">
                                        <muxc:ItemsRepeater.Layout>
                                            <muxc:StackLayout Orientation="Horizontal" Spacing="4"/>
                                        </muxc:ItemsRepeater.Layout>
                                        <muxc:ItemsRepeater.ItemTemplate>
                                            <DataTemplate x:DataType="x:String">
                                                <Border Background="{ThemeResource ControlFillColorTertiaryBrush}"
                                                        CornerRadius="2"
                                                        Padding="4,1">
                                                    <TextBlock Text="{x:Bind}" 
                                                               Style="{ThemeResource CaptionTextBlockStyle}"
                                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                               FontSize="10"/>
                                                </Border>
                                            </DataTemplate>
                                        </muxc:ItemsRepeater.ItemTemplate>
                                    </muxc:ItemsRepeater>

                                    <TextBlock Text="{x:Bind Id}"
                                               Style="{ThemeResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                               FontFamily="Consolas"
                                               FontSize="11"/>
                                </StackPanel>

                                <!-- Severity Badge -->
                                <Border Grid.Column="2"
                                        CornerRadius="4"
                                        Padding="8,4"
                                        VerticalAlignment="Center">
                                    <Border.Background>
                                        <SolidColorBrush Color="{x:Bind local:RulesPage.GetSeverityBackgroundColor(DefaultSeverity)}"/>
                                    </Border.Background>
                                    <TextBlock Text="{x:Bind SeverityDisplay}"
                                               FontSize="12"
                                               FontWeight="SemiBold">
                                        <TextBlock.Foreground>
                                            <SolidColorBrush Color="{x:Bind local:RulesPage.GetSeverityColor(DefaultSeverity)}"/>
                                        </TextBlock.Foreground>
                                    </TextBlock>
                                </Border>

                                <!-- Enable Toggle -->
                                <ToggleSwitch Grid.Column="3"
                                              IsOn="{x:Bind IsEnabled, Mode=TwoWay}"
                                              OnContent="Enabled"
                                              OffContent="Disabled"
                                              VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </muxc:ItemsRepeater.ItemTemplate>
            </muxc:ItemsRepeater>
        </ScrollViewer>

        <!-- Empty State -->
        <Border Grid.Row="2"
                Visibility="{x:Bind local:RulesPage.IsEmpty(ViewModel.FilteredRuleCount), Mode=OneWay}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="48"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel Spacing="12" HorizontalAlignment="Center">
                <FontIcon Glyph="&#xE8FD;" 
                          FontSize="48"
                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                <TextBlock Text="No rules match your filters"
                           Style="{ThemeResource SubtitleTextBlockStyle}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Try adjusting or clearing your filters"
                           Style="{ThemeResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
